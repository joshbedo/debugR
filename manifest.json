{
  "manifest_version": 2,
  "name": "debugR",
  "description": "A remote debugging tool built for inspecting elements on the page and returning errors from the console",
  "version": "0.1",
  "browser_action": {
    "default_icon": "icon.png",
    "default_popup": "foreground.html"
  }
}
